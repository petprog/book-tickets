import 'package:flutter/material.dart';

class ProfilePahe extends StatelessWidget {
  const ProfilePahe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
